@using AdvertisingPortal.Core.Models;
@model AdvertisementsViewModel

@{
    ViewData["Title"] = "Ogłoszenia";
}

<h1 class="mb-4">Ogłoszenia</h1>


<form id="filterForm" class="gy-2 gx-3 align-items-center mb-4">
    @Html.AntiForgeryToken()
    
    <div class="row">
        <div class="col-auto">
            @Html.TextBoxFor(x => x.FilterAdvertisements.Text,
            new { @class="form-control form-control mb-2",
            placeholder = "Wpisz poszukiwaną frazę..." })
        </div>
        <div class="col-auto">
            @Html.DropDownListFor(x => x.SortAdvertisements,
                                  Html.GetEnumSelectList<SortAdvertisements>(),
                                  null,
                                  new { @class = "form-control mb-2" })
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-primary">Szukaj</button>
        </div>
    </div>
    <div class="row">
        <div class="col-auto">
            <label class="form-label">Cena:</label>
        </div>
    </div>
    <div class="row mb-2">
        <div class="col-auto">
            @Html.TextBoxFor(x => x.FilterAdvertisements.PriceFrom,
                           new { @class="form-control", placeholder="Od"} )
        </div>
        <div class="col-auto">
            @Html.TextBoxFor(x => x.FilterAdvertisements.PriceTo,
                               new { @class="form-control", placeholder="Do"} )
         </div>  
    </div>
    <div class="row">
        <div class="col-auto">
            <label class="form-label">Kategorie:</label>
        </div>
    </div>
    <div class="row">
        <div class="col-auto d-flex flex-wrap">
            @foreach (var category in Model.FilterAdvertisements.Categories)
            {
                <div class="me-3 mb-2">
                    @Html.HiddenFor(x => category.CategoryId)

                    @Html.CheckBoxFor(x => category.IsSelected,
                                      new { @class="form-check-input" })

                    @Html.LabelFor(x => category,
                                   category.CategoryName,
                                   new { @class="form-check-label"} )
                </div>

                
            }
        </div>
    </div>
    
</form>

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col-12 d-flex justify-content-end">
            <button class="btn btn-sm btn-link" onclick="displayTable()">
                <i class="bi bi-table"></i>
            </button>
            <button class="btn btn-sm" onclick="displayCards()">
                <i class="bi bi-grid"></i>
            </button>          
        </div>
    </div>
    <div class="row">
        <div class="col-12 advertisements-partial">
            @await Html.PartialAsync("_AdvertisementsTable", Model.Advertisements)
        </div>
    </div>
    <div class="row">
        <div class="col-12 advertisements-partial">
            @await Html.PartialAsync("_AdvertisementsCards", Model.Advertisements)
        </div>
    </div>
</div>